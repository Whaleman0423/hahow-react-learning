
import { StyleSheet } from 'react-native';

const coloredBoxStyle = StyleSheet.create({
    baseBox: {
        width: 100,
        height: 100,
        borderWidth: 1,
        borderRadius: 10,
        borderColor: 'black',
        alignItems: 'center',
        justifyContent: 'center',
    }
})

export default coloredBoxStyle;
